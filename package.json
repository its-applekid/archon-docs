{
  "name": "archon-docs",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "build": "docbuild --site && node scripts/site-splash.mjs"
  },
  "dependencies": {
    "@aiur-team/docbuild": "0.0.1",
    "@netlify/blobs": "11.0.2",
    "@netlify/identity": "2.0.0"
  }
}
